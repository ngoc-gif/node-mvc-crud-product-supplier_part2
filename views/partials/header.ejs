<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Session Auth App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <a class="navbar-brand" href="/">Auth App</a>
    <div class="ms-auto">
      <% if (user) { %>
        <span class="text-white me-2">Xin chào <%= user.username %></span>
        <a href="/logout" class="btn btn-sm btn-danger">Đăng xuất</a>
      <% } else { %>
        <a href="/login" class="btn btn-sm btn-primary">Đăng nhập</a>
        <a href="/register" class="btn btn-sm btn-success">Đăng ký</a>
      <% } %>
    </div>
  </nav>
